/**
 * Яка модель Gemini читає чек — і як вимкнути їй роздуми.
 *
 * Окремим файлом, бо цю пару потрібно знати у двох місцях: сам маршрут
 * розпізнавання і перевірка `npm run check:gemini`, яка стукає в модель
 * справжнім запитом. Продублювати означало б одного дня хвалити не те, що
 * працює.
 *
 * Чому найдешевший ряд: тут потрібні очі, а не розум — переписати
 * надруковане й не домислювати. Різниця зі старшими моделями не в тому, чи
 * вони розберуть друк, а в ціні роздумів, яких ми не просимо.
 *
 * Чому саме 3.1, а не новіша 3.5 того ж ряду: за ту саму роботу вона дешевша
 * майже на третину — $0,25 проти $0,30 за мільйон вхідних і $1,50 проти $2,50
 * за мільйон вихідних (ai.google.dev/pricing, вересень 2026). На переписуванні
 * друку різниці в якості між ними немає: обидві «lite», і на тому самому
 * запиті дають однакову відповідь. Коли Google перестане давати 3.1 новим
 * ключам — так уже сталося з 2.5, — спадкоємиця саме 3.5-flash-lite, і тоді
 * разом із назвою треба поміняти поле роздумів нижче.
 *
 * Скільки це коштує насправді: фото — це десяток плиток 768×768, тобто
 * приблизно 2800 вхідних токенів разом із підказкою, і ще вісім сотень
 * вихідних на готовий JSON. Разом близько $0,002 за чек — тобто пів тисячі
 * чеків на долар.
 *
 * Назву перевіряй викликом, а не переліком моделей: `models.list` показує й
 * ті, яких новим ключам уже не дають. Саме так відпала gemini-2.5-flash-lite —
 * у списку є, а на запит 404.
 */
export const GEMINI_MODEL = "gemini-3.1-flash-lite";

/**
 * Вимкнені роздуми. Вони коштують вихідних токенів — найдорожчого, що тут є, —
 * а користі не дають: переписати рядок із чека нема над чим думати.
 *
 * Поле залежить від покоління моделі, і це не дрібниця: 2.5 і 3.1 приймають
 * `thinkingBudget: 0`, а 3.5 на нього відповідає 400 і хоче `thinkingLevel`
 * («off» вона теж не розуміє — найменше з дозволеного це «minimal»). Міняєш
 * модель — перевір пару через `npm run check:gemini`.
 */
export const GEMINI_THINKING = { thinkingBudget: 0 };
